using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Proyecto26;

public class Pruebibiris : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
        //RestClient.Post("https://proyecto-de-grado-7e7d3-default-rtdb.firebaseio.com/sesiones/.json", new Sesion { fecha = "12/03/2021", vidas = 3 }).Then(response => {
        //    print("siuuuu");
        //});
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
